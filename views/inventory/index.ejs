<% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<div class="links">
    <a href="classification/add">Add New Classification</a>
    <a href="inv/add-vehicle">Add New Vehicle</a>
</div>
